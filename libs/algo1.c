#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "model.h"
#include "cibles.h"

/*void algo1(Model *m,Cible *cibles)
{
    //Initialisation des cibles
    init_cibles(m,cibles);

    //on parcourt le modèle
    for (int i = 0; i < m->n_lignes; i++)
    {
        for (int j = 0; j < m->n_colonnes ; j++)
        {
            if (strcmp(m->tab[i][j],"o"==1)) //si on est pas sur un obstacle
        }
    }


}*/